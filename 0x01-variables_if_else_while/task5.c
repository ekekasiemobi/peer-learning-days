#include <stdio.h>
/**
 * main - prints numbers
 * prints a new line
 * Return: always 0 (success)
 */

int main(void)
{
	int a;

	for (a = 0 ; a < 10 ; a++)
	{
		printf("%i", a);
	}
		printf("\n");

	return (0);
}
